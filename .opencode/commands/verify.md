---
description: Run the standard local verification gate
agent: implementer
---

Execute `npm ci && npm run check`. If `npm run check` fails, run `npm run typecheck`, `npm run lint`, and `npm run build` individually to isolate causes.
